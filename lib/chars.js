/**
 * Get an array of characters from a string.
 *
 * @param {String} str - The string to split into characters.
 * @returns {String[]} - The provided string as an array of characters.
 */
module.exports = function chars(str) {
    return str.split('');
}
